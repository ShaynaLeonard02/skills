# skills

This repository stores the skills and agents that I create.

## Structure

- `/skills/` contains individual skills
- `/agents/` contains individual agents

## Adding new content

Create a new folder inside `/skills/` or `/agents/` for each item you add and keep any supporting files with it.